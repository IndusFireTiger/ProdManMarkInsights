# build output
dist/
# dependencies
node_modules/
# generated types
.astro/
# environment
.env
.env.production
# macOS / editors
.DS_Store

# local-only notes and source material (not published)
# This repo is PUBLIC. someInputs/ holds research PDFs including a form
# submission with a personal phone number and email; ToDo/ holds career and
# academy planning. Both live here for convenience, never in git history.
TODO.md
ToDo/
someInputs/
